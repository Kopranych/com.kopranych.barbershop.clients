spring:
  application:
    name: clients
  cloud:
    config:
      uri: http://localhost:8888
---
spring:
  profiles: local
eureka:
  client:
    enabled: false
---
spring:
  profiles: docker
  cloud:
    config:
      uri: http://barbershop-config:8888

