spring:
  application:
    name: clients
  cloud:
    config:
      uri: http://localhost:8888

  jpa:
    database: POSTGRESQL
    show-sql: false
    properties.hibernat e.dialect: org.hibernate.dialect.PostgreSQLDialect
    generate-ddl: true
  datasource:
    driver-class-name: org.postgresql.Driver
    platform: postgres
    url: jdbc:postgresql://localhost:5432/
    username: postgres
    password: example

eureka:
  client:
    enabled: false
